[pytest]
asyncio_mode = auto
markers =
    e2e: End-to-end tests
    datasource: Data source switching tests
    performance: Performance benchmark tests
    slow: Tests that take > 5s
    integration_real: Real integration tests (no mocks, real API calls)
